#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: - one of the numbers to be added together
 * @b: - the other number whose sum is to be found
 *
 * Return: The sum
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}

